- service: app
  command: bash -c "sleep 20; MIX_ENV=test mix test 1>&1"
- service: app
  type: push
  image_name: edevil/haypoll
  image_tag: "{{.ServiceName}}.{{.Branch}}.{{.Timestamp}}"
  registry: https://index.docker.io/v1/
  encrypted_dockercfg_path: dockercfg.encrypted
